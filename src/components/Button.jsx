import { ButtonStyle1 } from "../styles/style";
import styled from "styled-components";

const Button = styled.button`
  ${ButtonStyle1};
  justify-self: center;
  margin: 0 auto;
`;

export default Button;
